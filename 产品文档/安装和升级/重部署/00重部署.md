重部署是指对当前数据库服务端进行重新部署，根据不同的适用场景可分为：

- 在机房搬迁、网段重新划分等场景下，数据库服务端所在服务器的物理信息发生变化后，数据库所依赖的运行环境随之变更，管理员应及时采取措施对数据库进行[在线重新部署](更换服务器IP)，尽快恢复业务。

- 在某些特定场景下，例如需要定期清理测试环境的数据库并重装、反复学习从零开始数据库安装等，该类操作通常不涉及数据库安装的初始环境及相关配置变化、数据库版本变化，直接[在原环境卸载再重装](原环境卸载重装)即可。

