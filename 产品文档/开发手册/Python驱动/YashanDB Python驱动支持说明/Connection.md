## Connection连接对象

为数据库连接对象，对应Python应用程序与数据库的实际连接，用于执行整个数据库层面上的操作。

由python-yasdb模块提供的connect函数创建。

### 已支持的方法

| 方法       |
| ---------- |
| close()    |
| commit()   |
| rollback() |
| cursor()   |

