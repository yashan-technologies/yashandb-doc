YashanDB通过将数据库存储结构划分为逻辑存储结构和物理存储结构来提供高效、灵活、便捷的存储管理能力：

- [物理存储结构](物理存储结构)：数据在物理上的储存形式，通常为操作系统层面的物理文件。

- [逻辑存储结构](逻辑存储结构)：基于物理文件划分的逻辑结构，可以方便物理存储的管理。

将物理存储结构和逻辑存储结构分离后，YashanDB使用者可以分别对不同的结构进行管理而互相不会产生影响。例如，扩展数据文件不会影响存储在其中的表数据、删除表并不会影响实际存储的文件结构。