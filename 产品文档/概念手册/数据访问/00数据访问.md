访问存放在数据库中的数据需要使用遵循标准的数据访问语言，例如SQL。

SQL语言是一种声明性语言，主要关注数据的处理。

但实际基于SQL的开发场景中，经常需要将多条SQL组合成一个逻辑块，例如一个事务，相同业务的功能公共处理等。对此，YashanDB提供了两种能力，一种是提供了各驱动接口，结合客户开发工具和业界中间件一起完成业务。另一种是PL语言，PL语言直接在数据库内核提供开发平台，提供了基于SQL的数据处理能力。PL语句可以灵活自由的组合，以多种对象形式存储在数据库中，调用时减少了网络传输代价，既高效又灵活。