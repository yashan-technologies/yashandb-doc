闪回是指通过只回退对数据库对象进行的相关更改将数据回退到历史的某个时间点，无需执行传统的还原备份的恢复操作。

YashanDB支持基于数据库全库维度或基于指定某个表进行[闪回恢复](闪回恢复)，例如闪回到执行DROP某个表之前的时间点从而还原误删的表和数据，闪回操作过程不影响数据库的使用且不会产生额外的空间占用。

同时，也提供[闪回查询](闪回查询)功能，方便用户追溯历史版本的表数据。在满足相应条件下，在表数据被UPDATE或DELETE更改后，用户仍能获取到某个历史版本的数据。
