```ebnf
now = NOW ["()"].
```

NOW函数无给定参数，直接返回当前数据库所在的操作系统设置的当前日期，其返回类型为DATE，且与DATE\_FORMAT参数所指定格式一致。

如果一个SQL语句中出现了多个NOW函数，在该语句执行过程中每次都会调用NOW函数获取当前日期。

示例

```sql
SELECT NOW FROM DUAL;
NOW                              
-------------------------------- 
2022-08-24 23:55:31 
```
