initno:1

filename:condition,constraint（yashan模式）,constraint（mysql模式）,compile_clause,dblink,expr,external_table,hint,json,RegExp,size_clause,storage_clause

enName:condition,constraint (yashan Mode),constraint (mysql Mode),compile_clause,dblink,expr,external_table,hint,json,RegExp,size_clause,storage_clause
