单机（主备）部署包含如下具体的部署形态和场景：

| 部署形态| 部署场景|
| ------------------------------------------------------------ | ------------------------------------------------------------ |
| 单节点<br>一主一备<br>一主多备<br>级联备<br>双复制组主备     | 租户/非租户<br>yashan模式/mysql模式<br>单一网段/多网段       |



本文[第二部分](#installation)所述各部署形态的安装过程将默认按非租户模式、yashan模式、以及公网私网位于同一网段内的场景进行示例演示。请仔细阅读本文[第一部分](#InstallationScenario)对各种安装场景的说明，并参照指导对示例命令进行调整后再运行安装。

<span id="InstallationScenario" name="InstallationScenario" class="yaslink"></span>

## 安装场景说明

请确认您的安装场景，进入对应章节查看。

租户模式和mysql模式在产品安装部署时不可同时选择，如您需要构建mysql模式的多租户数据库，请在完成安装后，在根容器中指定mysql选项来创建mysql模式的PDB。

### 租户模式

按租户模式部署的YashanDB实例将被创建为容器数据库（CDB），用于统一管理多个可插拔数据库（PDB）。

如确定使用此模式安装，您需要在示例安装过程的【步骤1：生成配置文件】的对应命令中增加 **--enable-pluggable-database** 选项，例如：


```shell
$ ./bin/yasboot package se gen --cluster yashandb --recommend-param \
-u yashan -p password --ip 192.168.1.2 --port 22 \
--install-path /data/yashan/yasdb_home  \
--data-path /data/yashan/yasdb_data \
--log-path /data/yashan/log \
--begin-port 1688 \
--enable-pluggable-database
```



之后的步骤无区别。

指定 **--enable-pluggable-database** 选项后，生成的配置文件中的相关变化如下（yashandb.toml文件名称来自于示例安装命令中指定的集群名称，如您指定了不同的名称，此处文件名称将相应不同）：

::: tabs

== hosts.toml

```toml
...
create_cgroup = true        # CDB模式将会同步启用资源组管理
...
is_cdb = true				# 标识部署的数据库为CDB
...
[om]
...
```

== yashandb.toml

```toml
...
enable_cdb = true       # 标识部署的数据库为CDB
...
[[group]]
  ...
  [group.config]        
    ...
    [group.config.SEED]           # 此处将会出现内置模板PDB（PDB seed）的内置表空间配置
      SEED_SYSAUX_FILE = ""
      SEED_SYSAUX_SIZE = ""
      SEED_SYSTEM_FILE = ""
      SEED_SYSTEM_SIZE = ""

  [[group.node]]
      ...
      CGROUP_ROOT_DIR = "/sys/fs/cgroup"  # 数据库配置参数，cgroup目录配置为/sys/fs/cgroup，且不允许手动修改
      ENABLE_PLUGGABLE_DATABASE = "TRUE"  # 数据库配置参数，标识当前数据库为CDB
      ...
```
:::

### mysql模式

按mysql模式部署的YashanDB将会运行于与MySQL高度兼容的语法模式，安装完成后要求以MySQL而非YashanDB的语法格式才操作使用数据库。

如确定使用此模式安装，您需要在示例安装过程的【步骤1：生成配置文件】的对应命令中增加 **--mode mysql** 选项，例如：


```shell
$ ./bin/yasboot package se gen --cluster yashandb --recommend-param \
-u yashan -p yashan_password --ip 192.168.1.2 --port 22 \
--install-path /data/yashan/yasdb_home  \
--data-path /data/yashan/yasdb_data \
--log-path /data/yashan/log \
--begin-port 1688 \
--mode mysql
```


之后的步骤无区别。

指定 **--mode mysql** 选项后，生成的配置文件中的相关变化如下（yashandb.toml文件名称来自于示例安装命令中指定的集群名称，如您指定了不同的名称，此处文件名称将相应不同）：

::: tabs

== yashandb.toml

```toml
...
mode = "MYSQL"          # 数据库服务端语法模式被设置为MYSQL
...
table_type = "HEAP"     # 主要的业务表类型，mysql模式只能为HEAP表
...

[[group]]
  ...
  [group.config]        
    ...

  [[group.node]]
    ...
    mysql_addr = "192.168.1.2:1690" # MySQL协议的监听地址，仅mysql模式才有该参数
    ...
    [group.node.config] 
      ...
      EMPTY_STRING_AS_NULL = false  # 数据库配置参数，空字符串不等于NULL表示mysql模式，不允许手动修改
      ...
    [group.node.mysql_config] # 可在此项目下配置所有mysql模式下的全局变量
```
:::

### 多网段

请查阅[组网规划](安装前准备/网络准备)文档了解我们对YashanDB公网和私网的定义和划分建议。

如您在网络准备过程中，已将公网和私网分属到不同的物理网段（即一台服务器可能拥有不同网段的多个IP地址），这将会影响到示例安装过程的【步骤1：生成配置文件】的对应命令中如下安装参数的值选择：

- **--ip**：此参数值为安装登录服务器IP地址，也是默认的数据库对外监听地址，请设置为公网IP地址。
- **--listen-cidr**: 数据库监听地址所在网段，一般无需输入，采用默认值，即使用--ip输入IP地址所在的网段。

- **--replica-cidr**：此参数值为主备复制链路地址所在网段，在多网段场景中可将其设置为私网网段，以生成主备复制链路私网地址。

  当主备节点的主备复制地址各自位于不同网段（例如异地级联备或地双复制组主备）时，不可使用该选项，而应通过如下修改配置文件的方式分别进行指定（yashandb.toml文件名称来自于示例安装命令中指定的集群名称，如您指定了不同的名称，此处文件名称将相应不同）：

  ::: tabs
  == yashandb.toml

```toml
## 清空REPLICATION_ADDR参数中的公网地址，并在配置文件最下方增加下述信息，注意hostid与IP地址的对应
...
      REPLICATION_ADDR = ":1689"
...
      REPLICATION_ADDR = ":1689"
...
[[host]]
  hostid = "host0001"
  [host.yasagent]
  [host.yasdb_ip]
    replica_ip = "172.16.1.2"

[[host]]
  hostid = "host0002"
  [host.yasagent]
  [host.yasdb_ip]
    replica_ip = "172.16.1.3"
```
  :::

之后的步骤无区别。

<span id="installation" name="installation" class="yaslink"></span>

## 单节点部署

部署单节点形态的YashanDB将在一台指定的服务器上创建一个独立的数据库实例。


请以安装用户（以yashan为例）登录到已下载软件包的服务器（本文以192.168.1.2为例），并进入软件包所在的安装目录（以/home/yashan/install为例）。

```shell
$ cd /home/yashan/install
```


### 步骤1：生成配置文件


本步骤用于生成YashanDB产品的两个重要的配置文件：hosts.toml和{集群名称}.toml，其中，集群名称来自于--cluster选项的参数值，本文中为yashandb，该配置文件在下文均称为yashandb.toml。

hosts.toml和yashandb.toml中定义了YashanDB的部署信息和建库信息，也被用于产品之后的扩缩容和升级等操作，因此一旦确认（即部署完成并投入运行）后请不要进行手工调整。

在未进行下一步骤前，本步骤的命令可重复运行，每次运行将直接覆盖之前生成的hosts.toml和yashandb.toml文件。

1. 执行yasboot package se gen命令生成配置文件，该命令的参数全集请查阅[yasboot文档](../../全部手册/工具手册/yasboot/yasboot命令介绍/yasboot package)。

    以下所展示的命令示例包含了命令的必输入选项，以及我们建议您输入的选项，请根据实际情况自定义选项参数。

    ::: tabs
    == 默认示例场景

```shell
$ ./bin/yasboot package se gen --cluster yashandb --recommend-param \
-u yashan -p yashan_password --ip 192.168.1.2 --port 22 \
--install-path /data/yashan/yasdb_home  \
--data-path /data/yashan/yasdb_data \
--log-path /data/yashan/log \
--begin-port 1688 
```

    == 租户模式

```shell
$ ./bin/yasboot package se gen --cluster yashandb --recommend-param \
-u yashan -p password --ip 192.168.1.2 --port 22 \
--install-path /data/yashan/yasdb_home  \
--data-path /data/yashan/yasdb_data \
--log-path /data/yashan/log \
--begin-port 1688 \
--enable-pluggable-database
```

    == mysql模式

```shell
$ ./bin/yasboot package se gen --cluster yashandb --recommend-param \
-u yashan -p yashan_password --ip 192.168.1.2 --port 22 \
--install-path /data/yashan/yasdb_home  \
--data-path /data/yashan/yasdb_data \
--log-path /data/yashan/log \
--begin-port 1688 \
--mode mysql
```

    :::

    

    执行完毕后，当前目录下将生成hosts.toml和yashandb.toml两个配置文件。

2. 请确认您在上一步所输入的生成配置文件命令是否对应了您所需要的[安装场景](#InstallationScenario)。

    如需修改场景，可按对应场景的操作指导调整上一步所输入的生成配置文件命令，重新运行，再次生成配置文件。

3. 根据实际需要，手动修改配置文件中的相关参数。

    >**Caution**:
    >
    > 不建议删除文件中任何行，否则可能导致后续安装过程报错或所搭建的环境后续无法进行扩展配置。

    以下列示部分可能需要调整的参数项，完整的配置文件内容说明请查阅[配置文件模板](../../全部手册/工具手册/yasboot/配置文件/00配置文件)介绍，请根据实际情况选择参数项调整值。

    
    ::: tabs
    == hosts.toml



| 参数项| 调整说明|
| ------------------------------------------ | ------------------------------------------------------------ |
| 所有的LISTEN_ADDR                           | yasom服务和yasagent服务所在地址和监听端口，请检查确保与实际开放的端口号一致，可修改为实际值             |



    == yashandb.toml

| 参数项| 调整说明|
| ------------------------------------------ | ------------------------------------------------------------ |
| table_type                                 | 主要的业务表类型，默认HEAP，mysql模式不可调整                |
| recommend_param/scale_factor/scene         | 指定了--recommend-param选项时，可进一步调整推荐参数的选项，具体请参考[DBMS_PARAM](../../全部手册/开发手册/PL参考手册/内置高级包/DBMS_PARAM)高级包文档描述。 |
| mysql_addr                                 | yashan模式无此参数<br>MySQL协议的监听地址和端口，请检查确保与实际开放的端口号一致，可修改为实际值             |
| [group.config]下各参数                     | 该类参数均为[建库关键参数](../../全部手册/工具手册/yasboot/建库参数)，未在文件中列出的参数均采用默认值，生产环境请务必按实际情况进行调整。 |
| [group.node.config]下各参数                | 该类参数均为本节点所部署实例的[数据库配置参数](../../全部手册/参考手册/配置参数)，未在文件中列出的参数均采用默认值，生产环境请务必按实际情况进行调整。 |
| [group.config.SEED]下各参数                | 非租户模式无该类参数<br>该类参数用于定义种子容器的属性，默认继承根容器属性，如需自定义PDB创建模板，请在此处调整参数选项值。 |
| [group.node.mysql_config]下各参数          | 该类参数均为mysql模式下的[系统变量](../../全部手册/mysql模式参考手册/系统变量/系统变量参考)（yashan模式无需理会），未在文件中列出的参数均采用默认值，生产环境请务必按实际情况进行调整。 |

    :::

### 步骤2：执行安装


执行如下命令，执行软件包安装。

```shell
$ ./bin/yasboot package install -t hosts.toml
```

|  参数| 说明|
|--------------------|--------------------------------------|
| -t, --toml | 部署数据库集群的hosts.toml路径 |



### 步骤3：部署数据库


执行如下命令，部署数据库。

```shell
$ ./bin/yasboot cluster deploy -t yashandb.toml -p sys_password
```

|  参数| 说明|
|--------------------|--------------------------------------|
| -t, --toml | 部署数据库集群的{集群名称}.toml文件路径 |
| -p, --sys-password |设置数据库超级管理员sys用户的密码，配置要求如下：<br/>* 密码长度为8 - 64位<br/>* 密码中不能包含对应的数据库用户名称<br/>* 密码必须同时包含数字、字母和特殊字符<br/>* OS命令相关的特殊字符（例如`@`、`/`、`.`、`!`、`$`、`'`等）需进行转义  |

> **Note**:
>
> 还可以通过指定`-d, --child`参数展示任务以及子任务信息，以便了解部署进度。

返回如下信息表示已成功部署。

```shell
task completed, status: SUCCESS
```



### 步骤4：配置环境变量




部署成功后，--install-path（例如/data/yashan/yasdb_home）指定的目录下会生成子目录`{version_number}/conf`，该目录下会自动生成YashanDB相关的环境变量文件`{集群名称}.bashrc`，需将其应用于操作系统。

 执行如下命令，生效环境变量。

```shell
# 进入环境变量文件所在目录，例如/data/yashan/yasdb_home/{version_number}/conf
$ cd /data/yashan/yasdb_home/{version_number}/conf

# 生效环境变量
$ cat yashandb.bashrc >> ~/.bashrc
$ source ~/.bashrc

# 校验环境变量是否生效（回显信息中的路径请以实际为准）
$ echo $YASDB_DATA
/data/yashan/yasdb_data/db-1-1
```

具体的环境变量介绍请查阅[安装后初始环境 > 环境变量](../../全部手册/安装和升级/安装部署/安装后初始环境/环境变量)。



2. 以安装用户登录当前环境中的其他服务器，重复上述操作生效环境变量。


### 步骤5：检查安装结果


若连接报错或执行SQL语句报错，请根据错误提示信息检查安装步骤，或咨询我们的技术支持。

1. 执行如下命令，查看数据库状态。

    ```shell
    $ yasboot cluster status -c yashandb -d
    ```
        
    回显信息示例如下：
    
    :::tabs
    == 默认示例场景

```shell
+--------------------------------------------------------------------------------------------------------------------------------------------------------------+
| hostid   | node_type | nodeid | pid   | instance_status | database_status | database_role | listen_address    | source_node | data_path                      |
+--------------------------------------------------------------------------------------------------------------------------------------------------------------+
| host0001 | db        | 1-1:1  | 25283 | open            | normal          | primary       | 192.168.1.2:1688  | -           | /data/yashan/yasdb_data/db-1-1 |
+----------+-----------+--------+-------+-----------------+-----------------+---------------+-------------------+-------------+--------------------------------+
```
    == 租户模式

```shell
+-------------------------------------------------------------------------------------------------------------------------------------------------------------------+
| hostid   | nodeid | node_type | pdb_name | pid     | instance_status | pdb_status | pdb_role | source_node | listen_address      | data_path                      |
+-------------------------------------------------------------------------------------------------------------------------------------------------------------------+
| host0001 | 1-1:1  | cdb       | cdb$root | 1562565 | open            | open       | primary  | -           | 192.168.1.2:1688    | /data/yashan/yasdb_data/db-1-1 |
+----------+--------+-----------+----------+---------+-----------------+------------+----------+-------------+---------------------+--------------------------------+
```

    == mysql模式

```shell
+--------------------------------------------------------------------------------------------------------------------------------------------------------------+
| hostid   | node_type | nodeid | pid   | instance_status | database_status | database_role | listen_address    | source_node | data_path                      |
+--------------------------------------------------------------------------------------------------------------------------------------------------------------+
| host0001 | db        | 1-1:1  | 25283 | open            | normal          | primary       | 192.168.1.2:1688  | -           | /data/yashan/yasdb_data/db-1-1 |
+----------+-----------+--------+-------+-----------------+-----------------+---------------+-------------------+-------------+--------------------------------+
```
    :::



2. 使用[yasql](../../全部手册/工具手册/yasql/yasql使用指导)工具连接数据库，查看实例状态。

    ```shell
    $ yasql sys/********@192.168.1.2:1688
    SQL> SELECT STATUS FROM V$INSTANCE;

    STATUS        
    ------------- 
    OPEN        

    SQL> SELECT database_name FROM v$database;

    DATABASE_NAME                                                    
    ---------------------------------------------------------------- 
    yashandb   
    ```



### 步骤6：启动守护进程


执行如下命令，启动守护进程。

```shell
$ yasboot monit start --cluster yashandb
```

> **Note**:
>
> 后续使用中服务器重启开机后需手动启动守护进程，守护进程会自动拉起数据库相关进程，从而启动数据库。为简化运维，可将守护进程配置为开机自启动间接实现数据库开机自启动，具体操作请查阅[配置开机自启动](../../全部手册/安装和升级/安装部署/安装后初始环境/配置开机自启动)。



## 一主一备部署

部署一主一备形态的YashanDB将在两个服务器节点上创建数据库实例，分别为主库和备库角色，对外提供读写（主库）和只读（备库）服务。


请以安装用户（以yashan为例）登录到已下载软件包的服务器（本文以192.168.1.2为例），并进入软件包所在的安装目录（以/home/yashan/install为例）。

```shell
$ cd /home/yashan/install
```



### 步骤1：生成配置文件


本步骤用于生成YashanDB产品的两个重要的配置文件：hosts.toml和{集群名称}.toml，其中，集群名称来自于--cluster选项的参数值，本文中为yashandb，该配置文件在下文均称为yashandb.toml。

hosts.toml和yashandb.toml中定义了YashanDB的部署信息和建库信息，也被用于产品之后的扩缩容和升级等操作，因此一旦确认（即部署完成并投入运行）后请不要进行手工调整。

在未进行下一步骤前，本步骤的命令可重复运行，每次运行将直接覆盖之前生成的hosts.toml和yashandb.toml文件。

1. 执行yasboot package se gen命令生成配置文件，该命令的参数全集请查阅[yasboot文档](../../全部手册/工具手册/yasboot/yasboot命令介绍/yasboot package)。

    以下所展示的命令示例包含了命令的必输入选项，以及我们建议您输入的选项，请根据实际情况自定义选项参数。

    请注意--ip后输入的地址顺序决定了默认的主备角色，系统以输入的第一个IP地址作为主库节点。

    :::tabs
    == 默认示例场景

```shell
$ ./bin/yasboot package se gen --cluster yashandb \
-u yashan -p yashan_password --ip 192.168.1.2,192.168.1.3 --port 22 \
--install-path /data/yashan/yasdb_home  \
--data-path /data/yashan/yasdb_data \
--log-path /data/yashan/log \
--begin-port 1688 \
--node 2
```

    == 租户模式

```shell
$ ./bin/yasboot package se gen --cluster yashandb \
-u yashan -p yashan_password --ip 192.168.1.2,192.168.1.3 --port 22 \
--install-path /data/yashan/yasdb_home  \
--data-path /data/yashan/yasdb_data \
--log-path /data/yashan/log \
--begin-port 1688 \
--node 2 \
--enable-pluggable-database
```

    == mysql模式

```shell
$ ./bin/yasboot package se gen --cluster yashandb \
-u yashan -p yashan_password --ip 192.168.1.2,192.168.1.3 --port 22 \
--install-path /data/yashan/yasdb_home  \
--data-path /data/yashan/yasdb_data \
--log-path /data/yashan/log \
--begin-port 1688 \
--node 2 \
--mode mysql
```

    == 多网段

```shell
$ ./bin/yasboot package se gen --cluster yashandb \
-u yashan -p yashan_password --ip 192.168.1.2,192.168.1.3 --port 22 \
--install-path /data/yashan/yasdb_home  \
--data-path /data/yashan/yasdb_data \
--log-path /data/yashan/log \
--begin-port 1688 \
--node 2  \
--replica-cidr 172.16.1.0/24
```
    :::



    执行完毕后，当前目录下将生成hosts.toml和yashandb.toml两个配置文件。

2. 请确认您在上一步所输入的生成配置文件命令是否对应了您所需要的[安装场景](#InstallationScenario)。

    如需修改场景，可按对应场景的操作指导调整上一步所输入的生成配置文件命令，重新运行，再次生成配置文件。

3. 根据实际需要，手动修改配置文件中的相关参数。

    >**Caution**:
    >
    > 不建议删除文件中任何行，否则可能导致后续安装过程报错或所搭建的环境后续无法进行扩展配置。

    以下列示部分可能需要调整的参数项，完整的配置文件内容说明请查阅[配置文件模板](../../全部手册/工具手册/yasboot/配置文件/00配置文件)介绍，请根据实际情况选择参数项调整值。

    :::tabs
    == hosts.toml

| 参数项| 调整说明|
| ------------------------------------------ | ------------------------------------------------------------ |
| [om]下的hostid和LISTEN_ADDR         | yasom服务主机标识，IP地址和监听端口<br>请检查确保主机标识和IP地址为主库所在节点<br>请检查确保监听端口号与实际开放的端口号一致，可修改为实际值            |
| 所有[host.yasagent]下的LISTEN_ADDR         | yasagent服务IP地址和监听端口<br>请检查确保监听端口号与实际开放的端口号一致，可修改为实际值            |

    == yashandb.toml



| 参数项| 调整说明|
| ------------------------------------------ | ------------------------------------------------------------ |
| table_type                                 | 主要的业务表类型，默认HEAP，mysql模式不可调整                |
| recommend_param/scale_factor/scene         | 指定了--recommend-param选项时，可进一步调整推荐参数的选项，具体请参考[DBMS_PARAM](../../全部手册/开发手册/PL参考手册/内置高级包/DBMS_PARAM)高级包文档描述。 |
| data_path                                 | YAS_DATA环境变量对应值，默认各节点的数据路径完全相同，不建议调整<br>如主备节点的数据路径不一致，则必须同时对所有节点上的DB_FILE_NAME_CONVERT、REDO_FILE_NAME_CONVERT、DB_BUCKET_NAME_CONVERT参数值进行调整             |
| mysql_addr                                 | yashan模式无此参数<br>MySQL协议的监听地址和端口，请检查确保与实际开放的端口号一致，可修改为实际值             |
| role         | 该参数定义了节点实例的主备角色，默认以--ip选项输入的第一个值为主库，如发现与实际不一致，建议重新指定--ip选项运行生成配置文件命令            |
| [group.config]下各参数                     | 该类参数均为[建库关键参数](../../全部手册/工具手册/yasboot/建库参数)，未在文件中列出的参数均采用默认值，生产环境请务必按实际情况进行调整。 |
| [group.node.config]下各参数                | 该类参数均为本节点所部署实例的[数据库配置参数](../../全部手册/参考手册/配置参数)，除以下参数外未在文件中列出的参数均采用默认值，生产环境请务必按实际情况进行调整。 |
| [group.config.SEED]下各参数                | 非租户模式无该类参数<br>该类参数用于定义种子容器的属性，默认继承根容器属性，如需自定义PDB创建模板，请在此处调整参数选项值。 |
| [group.node.mysql_config]下各参数          | 该类参数均为mysql模式下的[系统变量](../../全部手册/mysql模式参考手册/系统变量/系统变量参考)（yashan模式无需理会），未在文件中列出的参数均采用默认值，生产环境请务必按实际情况进行调整。 |
| REPLICATION_ADDR    | 该参数定义了主备复制链路地址和端口<br>端口号默认为LISTEN_ADDR的端口号+1，请确保与实际开放的端口号一致，可修改为实际值<br>多网段场景中，该参数的地址部分须留空，例如":1689"           |


    :::

### 步骤2：执行安装


执行如下命令，执行软件包安装。

```shell
$ ./bin/yasboot package install -t hosts.toml
```

|  参数| 说明|
|--------------------|--------------------------------------|
| -t, --toml | 部署数据库集群的hosts.toml路径 |



### 步骤3：部署数据库


执行如下命令，部署数据库。

```shell
$ ./bin/yasboot cluster deploy -t yashandb.toml -p sys_password
```

|  参数| 说明|
|--------------------|--------------------------------------|
| -t, --toml | 部署数据库集群的{集群名称}.toml文件路径 |
| -p, --sys-password |设置数据库超级管理员sys用户的密码，配置要求如下：<br/>* 密码长度为8 - 64位<br/>* 密码中不能包含对应的数据库用户名称<br/>* 密码必须同时包含数字、字母和特殊字符<br/>* OS命令相关的特殊字符（例如`@`、`/`、`.`、`!`、`$`、`'`等）需进行转义  |

> **Note**:
>
> 还可以通过指定`-d, --child`参数展示任务以及子任务信息，以便了解部署进度。

返回如下信息表示已成功部署。

```shell
task completed, status: SUCCESS
```



### 步骤4：配置环境变量




部署成功后，--install-path（例如/data/yashan/yasdb_home）指定的目录下会生成子目录`{version_number}/conf`，该目录下会自动生成YashanDB相关的环境变量文件`{集群名称}.bashrc`，需将其应用于操作系统。

1. 执行如下命令，生效环境变量。

    ```shell
    # 进入环境变量文件所在目录，例如/data/yashan/yasdb_home/{version_number}/conf
    $ cd /data/yashan/yasdb_home/{version_number}/conf

    # 生效环境变量
    $ cat yashandb.bashrc >> ~/.bashrc
    $ source ~/.bashrc
    
    # 校验环境变量是否生效（回显信息中的路径请以实际为准）
    $ echo $YASDB_DATA
    /data/yashan/yasdb_data/db-1-1
    ```

    具体的环境变量介绍请查阅[安装后初始环境 > 环境变量](../../全部手册/安装和升级/安装部署/安装后初始环境/环境变量)。



2. 以安装用户登录当前环境中的其他服务器，重复上述操作生效环境变量。


### 步骤5：检查安装结果


若连接报错或执行SQL语句报错，请根据错误提示信息检查安装步骤，或咨询我们的技术支持。

1. 执行如下命令，查看数据库状态。

    ```shell
    $ yasboot cluster status -c yashandb -d
    ```
        
    回显信息示例如下：

    如下输出在多网段场景中完全一致。

    :::tabs
    == 默认示例场景

```shell
+--------------------------------------------------------------------------------------------------------------------------------------------------------------+
| hostid   | node_type | nodeid | pid   | instance_status | database_status | database_role | listen_address    | source_node | data_path                      |
+--------------------------------------------------------------------------------------------------------------------------------------------------------------+
| host0001 | db        | 1-1:1  | 25283 | open            | normal          | primary       | 192.168.1.2:1688  | -           | /data/yashan/yasdb_data/db-1-1 |
+----------+-----------+--------+-------+-----------------+-----------------+---------------+-------------------+-------------+--------------------------------+
| host0002 | db        | 1-2:2  | 31194 | open            | normal          | standby       | 192.168.1.3:1688  | 1-1:1       | /data/yashan/yasdb_data/db-1-2 |
+----------+-----------+--------+-------+-----------------+-----------------+---------------+-------------------+-------------+--------------------------------+
```
    == 租户模式

```shell
+-----------------------------------------------------------------------------------------------------------------------------------------------------------------+
| hostid   | nodeid | node_type | pdb_name | pid   | instance_status | pdb_status | pdb_role | source_node | listen_address    | data_path                        |
+-----------------------------------------------------------------------------------------------------------------------------------------------------------------+
| host0001 | 1-1:1  | cdb       | cdb$root | 25348 | open            | open       | primary  | -           | 192.168.1.2:1688  | /data/yashan/yasdb_data/db-1-1   |
+----------+--------+-----------+----------+-------+-----------------+------------+----------+-------------+-------------------+----------------------------------+
| host0002 | 1-2:2  | cdb       | cdb$root | 29213 | open            | open       | standby  | 1-1:1       | 192.168.1.3:1688  | /data/yashan/yasdb_data/db-1-2   |
+----------+--------+-----------+----------+-------+-----------------+------------+----------+-------------+-------------------+----------------------------------+
```

    == mysql模式

```shell
+--------------------------------------------------------------------------------------------------------------------------------------------------------------+
| hostid   | node_type | nodeid | pid   | instance_status | database_status | database_role | listen_address    | source_node | data_path                      |
+--------------------------------------------------------------------------------------------------------------------------------------------------------------+
| host0001 | db        | 1-1:1  | 25283 | open            | normal          | primary       | 192.168.1.2:1688  | -           | /data/yashan/yasdb_data/db-1-1 |
+----------+-----------+--------+-------+-----------------+-----------------+---------------+-------------------+-------------+--------------------------------+
| host0002 | db        | 1-2:2  | 31194 | open            | normal          | standby       | 192.168.1.3:1688  | 1-1:1       | /data/yashan/yasdb_data/db-1-2 |
+----------+-----------+--------+-------+-----------------+-----------------+---------------+-------------------+-------------+--------------------------------+
```
    :::



2. 使用[yasql](../../全部手册/工具手册/yasql/yasql使用指导)工具连接数据库，查看实例状态。

    ```shell
    $ yasql sys/********@192.168.1.2:1688
    SQL> SELECT STATUS FROM V$INSTANCE;

    STATUS        
    ------------- 
    OPEN        

    SQL> SELECT database_name FROM v$database;

    DATABASE_NAME                                                    
    ---------------------------------------------------------------- 
    yashandb   
    ```



### 步骤6：启动守护进程


执行如下命令，启动守护进程。

```shell
$ yasboot monit start --cluster yashandb
```

> **Note**:
>
> 后续使用中服务器重启开机后需手动启动守护进程，守护进程会自动拉起数据库相关进程，从而启动数据库。为简化运维，可将守护进程配置为开机自启动间接实现数据库开机自启动，具体操作请查阅[配置开机自启动](../../全部手册/安装和升级/安装部署/安装后初始环境/配置开机自启动)。



### 步骤7：开启自动选主（可选）

在一主一备的部署形态下，如需开启自动选主，请参照[一主一备yasom仲裁选主](../../全部手册/高可用/自动选主配置/一主一备yasom仲裁选主)文档中所述操作指导进行配置。

## 一主多备部署

部署一主多备形态的YashanDB将在多个服务器节点上创建数据库实例，分别为主库和备库角色，对外提供读写（主库）和只读（备库）服务。


请以安装用户（以yashan为例）登录到已下载软件包的服务器（本文以192.168.1.2为例），并进入软件包所在的安装目录（以/home/yashan/install为例）。

```shell
$ cd /home/yashan/install
```



### 步骤1：生成配置文件


本步骤用于生成YashanDB产品的两个重要的配置文件：hosts.toml和{集群名称}.toml，其中，集群名称来自于--cluster选项的参数值，本文中为yashandb，该配置文件在下文均称为yashandb.toml。

hosts.toml和yashandb.toml中定义了YashanDB的部署信息和建库信息，也被用于产品之后的扩缩容和升级等操作，因此一旦确认（即部署完成并投入运行）后请不要进行手工调整。

在未进行下一步骤前，本步骤的命令可重复运行，每次运行将直接覆盖之前生成的hosts.toml和yashandb.toml文件。

1. 执行yasboot package se gen命令生成配置文件，该命令的参数全集请查阅[yasboot文档](../../全部手册/工具手册/yasboot/yasboot命令介绍/yasboot package)。

    以下所展示的命令示例包含了命令的必输入选项，以及我们建议您输入的选项，请根据实际情况自定义选项参数。

    请注意--ip后输入的地址顺序决定了默认的主备角色，系统以输入的第一个IP地址作为主库节点。多个IP地址以`,`逐一输入，连续IP地址则可以使用范围格式输入，例如192.168.1.[2-4]。

    以下命令以一主两备为例，请根据实际情况调整参数。

    :::tabs
    == 默认示例场景

```shell
$ ./bin/yasboot package se gen --cluster yashandb --recommend-param \
-u yashan -p yashan_password --ip 192.168.1.2,192.168.1.3,192.168.1.4 --port 22 \
--install-path /data/yashan/yasdb_home  \
--data-path /data/yashan/yasdb_data \
--log-path /data/yashan/log \
--begin-port 1688 --node 3
```

    == 租户模式

```shell
$ ./bin/yasboot package se gen --cluster yashandb --recommend-param \
-u yashan -p yashan_password --ip 192.168.1.2,192.168.1.3,192.168.1.4 --port 22 \
--install-path /data/yashan/yasdb_home  \
--data-path /data/yashan/yasdb_data \
--log-path /data/yashan/log \
--begin-port 1688 --node 3 \
--enable-pluggable-database
```

    == mysql模式

```shell
$ ./bin/yasboot package se gen --cluster yashandb --recommend-param \
-u yashan -p yashan_password --ip 192.168.1.2,192.168.1.3,192.168.1.4 --port 22 \
--install-path /data/yashan/yasdb_home  \
--data-path /data/yashan/yasdb_data \
--log-path /data/yashan/log \
--begin-port 1688 --node 3 \
--mode mysql
```

    == 多网段

```shell
$ ./bin/yasboot package se gen --cluster yashandb --recommend-param \
-u yashan -p yashan_password --ip 192.168.1.2,192.168.1.3,192.168.1.4 --port 22 \
--install-path /data/yashan/yasdb_home  \
--data-path /data/yashan/yasdb_data \
--log-path /data/yashan/log \
--begin-port 1688 --node 3 \
--replica-cidr 172.16.1.0/24
```
    :::



    执行完毕后，当前目录下将生成hosts.toml和yashandb.toml两个配置文件。

2. 请确认您在上一步所输入的生成配置文件命令是否对应了您所需要的[安装场景](#InstallationScenario)。

    如需修改场景，可按对应场景的操作指导调整上一步所输入的生成配置文件命令，重新运行，再次生成配置文件。

3. 根据实际需要，手动修改配置文件中的相关参数。

    >**Caution**:
    >
    > 不建议删除文件中任何行，否则可能导致后续安装过程报错或所搭建的环境后续无法进行扩展配置。

    以下列示部分可能需要调整的参数项，完整的配置文件内容说明请查阅[配置文件模板](../../全部手册/工具手册/yasboot/配置文件/00配置文件)介绍，请根据实际情况选择参数项调整值。

    :::tabs
    == hosts.toml



| 参数项| 调整说明|
| ------------------------------------------ | ------------------------------------------------------------ |
| 所有的LISTEN_ADDR                           | yasom服务和yasagent服务所在地址和监听端口，请检查确保与实际开放的端口号一致，可修改为实际值             |



    == yashandb.toml



| 参数项| 调整说明|
| ------------------------------------------ | ------------------------------------------------------------ |
| table_type                                 | 主要的业务表类型，默认HEAP，mysql模式不可调整                |
| recommend_param/scale_factor/scene         | 指定了--recommend-param选项时，可进一步调整推荐参数的选项，具体请参考[DBMS_PARAM](../../全部手册/开发手册/PL参考手册/内置高级包/DBMS_PARAM)高级包文档描述。 |
| data_path                                 | YAS_DATA环境变量对应值，默认各节点的数据路径完全相同，不建议调整<br>如主备节点的数据路径不一致，则必须同时对所有节点上的DB_FILE_NAME_CONVERT、REDO_FILE_NAME_CONVERT、DB_BUCKET_NAME_CONVERT参数值进行调整             |
| mysql_addr                                 | yashan模式无此参数<br>MySQL协议的监听地址和端口，请检查确保与实际开放的端口号一致，可修改为实际值             |
| role         | 该参数定义了节点实例的主备角色，默认以--ip选项输入的第一个值为主库，如发现与实际不一致，建议重新指定--ip选项运行生成配置文件命令            |
| [group.config]下各参数                     | 该类参数均为[建库关键参数](../../全部手册/工具手册/yasboot/建库参数)，未在文件中列出的参数均采用默认值，生产环境请务必按实际情况进行调整。 |
| [group.node.config]下各参数                | 该类参数均为本节点所部署实例的[数据库配置参数](../../全部手册/参考手册/配置参数)，除以下参数外未在文件中列出的参数均采用默认值，生产环境请务必按实际情况进行调整。 |
| [group.config.SEED]下各参数                | 非租户模式无该类参数<br>该类参数用于定义种子容器的属性，默认继承根容器属性，如需自定义PDB创建模板，请在此处调整参数选项值。 |
| [group.node.mysql_config]下各参数          | 该类参数均为mysql模式下的[系统变量](../../全部手册/mysql模式参考手册/系统变量/系统变量参考)（yashan模式无需理会），未在文件中列出的参数均采用默认值，生产环境请务必按实际情况进行调整。 |
| REPLICATION_ADDR    | 该参数定义了主备复制链路地址和端口<br>端口号默认为LISTEN_ADDR的端口号+1，请确保与实际开放的端口号一致，可修改为实际值<br>多网段场景中，该参数的地址部分须留空，例如":1689"           |



> **Note**:
> 
> HA_ELECTION_ENABLED参数为自动选主功能的总开关，不可直接在本处修改，部署一主多备时该参数自动且强制开启。如需关闭只能在安装完成后参照[一主多备自动选主](../../全部手册/高可用/自动选主配置/一主多备自动选主)文档手动操作。
    :::

### 步骤2：执行安装


执行如下命令，执行软件包安装。

```shell
$ ./bin/yasboot package install -t hosts.toml
```

|  参数| 说明|
|--------------------|--------------------------------------|
| -t, --toml | 部署数据库集群的hosts.toml路径 |



### 步骤3：部署数据库


执行如下命令，部署数据库。

```shell
$ ./bin/yasboot cluster deploy -t yashandb.toml -p sys_password
```

|  参数| 说明|
|--------------------|--------------------------------------|
| -t, --toml | 部署数据库集群的{集群名称}.toml文件路径 |
| -p, --sys-password |设置数据库超级管理员sys用户的密码，配置要求如下：<br/>* 密码长度为8 - 64位<br/>* 密码中不能包含对应的数据库用户名称<br/>* 密码必须同时包含数字、字母和特殊字符<br/>* OS命令相关的特殊字符（例如`@`、`/`、`.`、`!`、`$`、`'`等）需进行转义  |

> **Note**:
>
> 还可以通过指定`-d, --child`参数展示任务以及子任务信息，以便了解部署进度。

返回如下信息表示已成功部署。

```shell
task completed, status: SUCCESS
```



### 步骤4：配置环境变量




部署成功后，--install-path（例如/data/yashan/yasdb_home）指定的目录下会生成子目录`{version_number}/conf`，该目录下会自动生成YashanDB相关的环境变量文件`{集群名称}.bashrc`，需将其应用于操作系统。

1. 执行如下命令，生效环境变量。

    ```shell
    # 进入环境变量文件所在目录，例如/data/yashan/yasdb_home/{version_number}/conf
    $ cd /data/yashan/yasdb_home/{version_number}/conf

    # 生效环境变量
    $ cat yashandb.bashrc >> ~/.bashrc
    $ source ~/.bashrc
    
    # 校验环境变量是否生效（回显信息中的路径请以实际为准）
    $ echo $YASDB_DATA
    /data/yashan/yasdb_data/db-1-1
    ```

    具体的环境变量介绍请查阅[安装后初始环境 > 环境变量](../../全部手册/安装和升级/安装部署/安装后初始环境/环境变量)。



2. 以安装用户登录当前环境中的其他服务器，重复上述操作生效环境变量。


### 步骤5：检查安装结果


若连接报错或执行SQL语句报错，请根据错误提示信息检查安装步骤，或咨询我们的技术支持。

1. 执行如下命令，查看数据库状态。

    ```shell
    $ yasboot cluster status -c yashandb -d
    ```
        
    回显信息示例如下：

    如下输出在多网段场景中完全一致。

    :::tabs
    == 默认示例场景

```shell
+--------------------------------------------------------------------------------------------------------------------------------------------------------------+
| hostid   | node_type | nodeid | pid   | instance_status | database_status | database_role | listen_address    | source_node | data_path                      |
+--------------------------------------------------------------------------------------------------------------------------------------------------------------+
| host0001 | db        | 1-1:1  | 25283 | open            | normal          | primary       | 192.168.1.2:1688  | -           | /data/yashan/yasdb_data/db-1-1 |
+----------+-----------+--------+-------+-----------------+-----------------+---------------+-------------------+-------------+--------------------------------+
| host0002 | db        | 1-2:2  | 31194 | open            | normal          | standby       | 192.168.1.3:1688  | 1-1:1       | /data/yashan/yasdb_data/db-1-2 |
+----------+-----------+--------+-------+-----------------+-----------------+---------------+-------------------+-------------+--------------------------------+
| host0003 | db        | 1-3:3  | 13384 | open            | normal          | standby       | 192.168.1.4:1688  | 1-1:1       | /data/yashan/yasdb_data/db-1-3 |
+----------+-----------+--------+-------+-----------------+-----------------+---------------+-------------------+-------------+--------------------------------+
```
    == 租户模式

```shell
+-----------------------------------------------------------------------------------------------------------------------------------------------------------------+
| hostid   | nodeid | node_type | pdb_name | pid   | instance_status | pdb_status | pdb_role | source_node | listen_address    | data_path                        |
+-----------------------------------------------------------------------------------------------------------------------------------------------------------------+
| host0001 | 1-1:1  | cdb       | cdb$root | 25348 | open            | normal       | primary  | -           | 192.168.1.2:1688  | /data/yashan/yasdb_data/db-1-1 |
+----------+--------+-----------+----------+-------+-----------------+------------+----------+-------------+-------------------+----------------------------------+
| host0002 | 1-2:2  | cdb       | cdb$root | 29213 | open            | normal       | standby  | 1-1:1       | 192.168.1.3:1688  | /data/yashan/yasdb_data/db-1-2 |
+----------+--------+-----------+----------+-------+-----------------+------------+----------+-------------+-------------------+----------------------------------+
| host0003 | 1-3:3  | cdb       | cdb$root | 49582 | open            | normal       | standby  | 1-1:1       | 192.168.1.4:1688  | /data/yashan/yasdb_data/db-1-3 |
+----------+--------+-----------+----------+-------+-----------------+------------+----------+-------------+-------------------+----------------------------------+
```

    == mysql模式

```shell
+--------------------------------------------------------------------------------------------------------------------------------------------------------------+
| hostid   | node_type | nodeid | pid   | instance_status | database_status | database_role | listen_address    | source_node | data_path                      |
+--------------------------------------------------------------------------------------------------------------------------------------------------------------+
| host0001 | db        | 1-1:1  | 25283 | open            | normal          | primary       | 192.168.1.2:1688  | -           | /data/yashan/yasdb_data/db-1-1 |
+----------+-----------+--------+-------+-----------------+-----------------+---------------+-------------------+-------------+--------------------------------+
| host0002 | db        | 1-2:2  | 31194 | open            | normal          | standby       | 192.168.1.3:1688  | 1-1:1       | /data/yashan/yasdb_data/db-1-2 |
+----------+-----------+--------+-------+-----------------+-----------------+---------------+-------------------+-------------+--------------------------------+
| host0003 | db        | 1-3:3  | 13384 | open            | normal          | standby       | 192.168.1.4:1688  | 1-1:1       | /data/yashan/yasdb_data/db-1-3 |
+----------+-----------+--------+-------+-----------------+-----------------+---------------+-------------------+-------------+--------------------------------+
```
    :::



2. 使用[yasql](../../全部手册/工具手册/yasql/yasql使用指导)工具连接数据库，查看实例状态。

    ```shell
    $ yasql sys/********@192.168.1.2:1688
    SQL> SELECT STATUS FROM V$INSTANCE;

    STATUS        
    ------------- 
    OPEN        

    SQL> SELECT database_name FROM v$database;

    DATABASE_NAME                                                    
    ---------------------------------------------------------------- 
    yashandb   
    ```



### 步骤6：启动守护进程


执行如下命令，启动守护进程。

```shell
$ yasboot monit start --cluster yashandb
```

> **Note**:
>
> 后续使用中服务器重启开机后需手动启动守护进程，守护进程会自动拉起数据库相关进程，从而启动数据库。为简化运维，可将守护进程配置为开机自启动间接实现数据库开机自启动，具体操作请查阅[配置开机自启动](../../全部手册/安装和升级/安装部署/安装后初始环境/配置开机自启动)。



## 级联备部署

部署带级联备的主备形态时，级联备与所联结的备库构成了一层新的主备关系，但均为只读数据库。

可以为不同的备库分别建立级联备，以及建立多层的级联备，但在安装部署过程中，只可对一个备库进行一层的级联备部署，完成后可参照[级联备扩容文档](../../全部手册/安装和升级/扩缩容/单机主备扩缩容/级联备扩缩容)所述操作指导，将其他的级联备加入到本数据库集群中。


请以安装用户（以yashan为例）登录到已下载软件包的服务器（本文以192.168.1.2为例），并进入软件包所在的安装目录（以/home/yashan/install为例）。

```shell
$ cd /home/yashan/install
```



### 步骤1：生成配置文件


本步骤用于生成YashanDB产品的两个重要的配置文件：hosts.toml和{集群名称}.toml，其中，集群名称来自于--cluster选项的参数值，本文中为yashandb，该配置文件在下文均称为yashandb.toml。

hosts.toml和yashandb.toml中定义了YashanDB的部署信息和建库信息，也被用于产品之后的扩缩容和升级等操作，因此一旦确认（即部署完成并投入运行）后请不要进行手工调整。

在未进行下一步骤前，本步骤的命令可重复运行，每次运行将直接覆盖之前生成的hosts.toml和yashandb.toml文件。

1. 执行yasboot package se gen命令生成配置文件，该命令的参数全集请查阅[yasboot文档](../../全部手册/工具手册/yasboot/yasboot命令介绍/yasboot package)。

    以下所展示的命令示例包含了命令的必输入选项，以及我们建议您输入的选项，请根据实际情况自定义选项参数。

    请注意--ip选项后面的值应按照主、备、级联备的顺序进行输入。多个IP地址以`,`逐一输入，连续IP地址则可以使用范围格式输入，例如192.168.1.[2-4]。

    此外，--node选项的值将作为主+备的数量，--cascade-node选项的值将作为级联备的数量，--cascade-parent选项的值用于定位级联备的上级备库，该值表示--ip选项所输入IP地址从0开始的索引。

    以下命令以一主一备一级联备为例，请根据实际情况调整参数。

    :::tabs
    == 默认示例场景

```shell
$ ./bin/yasboot package se gen --cluster yashandb \
-u yashan -p yashan_password --ip 192.168.1.[2-4] --port 22 \
--install-path /data/yashan/yasdb_home  \
--data-path /data/yashan/yasdb_data \
--log-path /data/yashan/log \
--begin-port 1688 \
--node 2 \
--cascade-node 1 --cascade-parent 1
```

    == 租户模式

```shell
$ ./bin/yasboot package se gen --cluster yashandb \
-u yashan -p yashan_password --ip 192.168.1.[2-4] --port 22 \
--install-path /data/yashan/yasdb_home  \
--data-path /data/yashan/yasdb_data \
--log-path /data/yashan/log \
--begin-port 1688 \
--node 2 \
--cascade-node 1 --cascade-parent 1 \
--enable-pluggable-database
```

    == mysql模式

```shell
$ ./bin/yasboot package se gen --cluster yashandb \
-u yashan -p yashan_password --ip 192.168.1.[2-4] --port 22 \
--install-path /data/yashan/yasdb_home  \
--data-path /data/yashan/yasdb_data \
--log-path /data/yashan/log \
--begin-port 1688 \
--node 2 \
--cascade-node 1 --cascade-parent 1 \
--mode mysql
```

    == 多网段

主-备-级联备复制链路地址处于同一网段时，使用--replica-cidr指定网段；否则应在配置文件中进行手工调整。

```shell
$ ./bin/yasboot package se gen --cluster yashandb \
-u yashan -p yashan_password --ip 192.168.1.[2-4] --port 22 \
--install-path /data/yashan/yasdb_home  \
--data-path /data/yashan/yasdb_data \
--log-path /data/yashan/log \
--begin-port 1688 \
--node 2 \
--cascade-node 1 --cascade-parent 1 \
--replica-cidr 172.16.1.0/24
```
    :::



    执行完毕后，当前目录下将生成hosts.toml和yashandb.toml两个配置文件。

2. 请确认您在上一步所输入的生成配置文件命令是否对应了您所需要的[安装场景](#InstallationScenario)。

    如需修改场景，可按对应场景的操作指导调整上一步所输入的生成配置文件命令，重新运行，再次生成配置文件。

3. 根据实际需要，手动修改配置文件中的相关参数。

    >**Caution**:
    >
    > 不建议删除文件中任何行，否则可能导致后续安装过程报错或所搭建的环境后续无法进行扩展配置。

    以下列示部分可能需要调整的参数项，完整的配置文件内容说明请查阅[配置文件模板](../../全部手册/工具手册/yasboot/配置文件/00配置文件)介绍，请根据实际情况选择参数项调整值。

    :::tabs
    == hosts.toml



| 参数项| 调整说明|
| ------------------------------------------ | ------------------------------------------------------------ |
| 所有的LISTEN_ADDR                           | yasom服务和yasagent服务所在地址和监听端口，请检查确保与实际开放的端口号一致，可修改为实际值             |



    == yashandb.toml



| 参数项| 调整说明|
| ------------------------------------------ | ------------------------------------------------------------ |
| table_type                                 | 主要的业务表类型，默认HEAP，mysql模式不可调整                |
| recommend_param/scale_factor/scene         | 指定了--recommend-param选项时，可进一步调整推荐参数的选项，具体请参考[DBMS_PARAM](../../全部手册/开发手册/PL参考手册/内置高级包/DBMS_PARAM)高级包文档描述。 |
| data_path                                 | YAS_DATA环境变量对应值，默认各节点的数据路径完全相同，不建议调整<br>如主备节点的数据路径不一致，则必须同时对所有节点上的DB_FILE_NAME_CONVERT、REDO_FILE_NAME_CONVERT、DB_BUCKET_NAME_CONVERT参数值进行调整             |
| mysql_addr                                 | yashan模式无此参数<br>MySQL协议的监听地址和端口，请检查确保与实际开放的端口号一致，可修改为实际值             |
| role         | 该参数定义了节点实例的主备和级联备角色，如发现与实际不一致，建议重新指定--ip、--node、--cascade-node以及--cascade-parent选项运行生成配置文件命令            |
| [group.config]下各参数                     | 该类参数均为[建库关键参数](../../全部手册/工具手册/yasboot/建库参数)，未在文件中列出的参数均采用默认值，生产环境请务必按实际情况进行调整。 |
| [group.node.config]下各参数                | 该类参数均为本节点所部署实例的[数据库配置参数](../../全部手册/参考手册/配置参数)，除以下参数外未在文件中列出的参数均采用默认值，生产环境请务必按实际情况进行调整。 |
| [group.config.SEED]下各参数                | 非租户模式无该类参数<br>该类参数用于定义种子容器的属性，默认继承根容器属性，如需自定义PDB创建模板，请在此处调整参数选项值。 |
| [group.node.mysql_config]下各参数          | 该类参数均为mysql模式下的[系统变量](../../全部手册/mysql模式参考手册/系统变量/系统变量参考)（yashan模式无需理会），未在文件中列出的参数均采用默认值，生产环境请务必按实际情况进行调整。 |
| REPLICATION_ADDR    | 该参数定义了主备复制链路地址和端口<br>端口号默认为LISTEN_ADDR的端口号+1，请确保与实际开放的端口号一致，可修改为实际值<br>多网段场景中，该参数的地址部分须留空，例如":1689"           |
| [[host]]下各参数    | 未指定--replica-cidr时，配置文件中不会生成该类参数<br>指定了每个节点的私网IP地址，可手工增加该类参数，或对已生成的参数进行调整        |


    :::
 
### 步骤2：执行安装


执行如下命令，执行软件包安装。

```shell
$ ./bin/yasboot package install -t hosts.toml
```

|  参数| 说明|
|--------------------|--------------------------------------|
| -t, --toml | 部署数据库集群的hosts.toml路径 |



### 步骤3：部署数据库


执行如下命令，部署数据库。

```shell
$ ./bin/yasboot cluster deploy -t yashandb.toml -p sys_password
```

|  参数| 说明|
|--------------------|--------------------------------------|
| -t, --toml | 部署数据库集群的{集群名称}.toml文件路径 |
| -p, --sys-password |设置数据库超级管理员sys用户的密码，配置要求如下：<br/>* 密码长度为8 - 64位<br/>* 密码中不能包含对应的数据库用户名称<br/>* 密码必须同时包含数字、字母和特殊字符<br/>* OS命令相关的特殊字符（例如`@`、`/`、`.`、`!`、`$`、`'`等）需进行转义  |

> **Note**:
>
> 还可以通过指定`-d, --child`参数展示任务以及子任务信息，以便了解部署进度。

返回如下信息表示已成功部署。

```shell
task completed, status: SUCCESS
```



### 步骤4：配置环境变量




部署成功后，--install-path（例如/data/yashan/yasdb_home）指定的目录下会生成子目录`{version_number}/conf`，该目录下会自动生成YashanDB相关的环境变量文件`{集群名称}.bashrc`，需将其应用于操作系统。

1. 执行如下命令，生效环境变量。

    ```shell
    # 进入环境变量文件所在目录，例如/data/yashan/yasdb_home/{version_number}/conf
    $ cd /data/yashan/yasdb_home/{version_number}/conf

    # 生效环境变量
    $ cat yashandb.bashrc >> ~/.bashrc
    $ source ~/.bashrc
    
    # 校验环境变量是否生效（回显信息中的路径请以实际为准）
    $ echo $YASDB_DATA
    /data/yashan/yasdb_data/db-1-1
    ```

    具体的环境变量介绍请查阅[安装后初始环境 > 环境变量](../../全部手册/安装和升级/安装部署/安装后初始环境/环境变量)。



2. 以安装用户登录当前环境中的其他服务器，重复上述操作生效环境变量。


### 步骤5：检查安装结果


若连接报错或执行SQL语句报错，请根据错误提示信息检查安装步骤，或咨询我们的技术支持。

1. 执行如下命令，查看数据库状态。

    ```shell
    $ yasboot cluster status -c yashandb -d
    ```
        
    回显信息示例如下：

    如下输出在多网段场景中完全一致。

    :::tabs
    == 默认示例场景

```shell
+--------------------------------------------------------------------------------------------------------------------------------------------------------------+
| hostid   | node_type | nodeid | pid   | instance_status | database_status | database_role | source_node | listen_address  | data_path                        |
+--------------------------------------------------------------------------------------------------------------------------------------------------------------+
| host0001 | db        | 1-1:1  | 73152 | open            | normal          | primary       | -           | 192.168.1.2:1688 | /data/yashan/yasdb_data/db-1-1  |
+----------+-----------+--------+-------+-----------------+-----------------+---------------+-------------+------------------+---------------------------------+
| host0002 | db        | 1-2:2  | 73211 | open            | normal          | standby       | 1-1:1       | 192.168.1.3:1688 | /data/yashan/yasdb_data/db-1-2  |
+----------+-----------+--------+-------+-----------------+-----------------+---------------+-------------+------------------+---------------------------------+
| host0003 | db        | 1-3:3  | 73403 | open            | normal          | standby       | 1-2:2       | 192.168.1.4:1688 | /data/yashan/yasdb_data/db-1-3  |
+----------+-----------+--------+-------+-----------------+-----------------+---------------+-------------+------------------+---------------------------------+
```
    == 租户模式

```shell
+-----------------------------------------------------------------------------------------------------------------------------------------------------------------+
| hostid   | nodeid | node_type | pdb_name | pid   | instance_status | pdb_status | pdb_role | source_node | listen_address    | data_path                        |
+-----------------------------------------------------------------------------------------------------------------------------------------------------------------+
| host0001 | 1-1:1  | cdb       | cdb$root | 25348 | open            | normal       | primary  | -           | 192.168.1.2:1688  | /data/yashan/yasdb_data/db-1-1   |
+----------+--------+-----------+----------+-------+-----------------+------------+----------+-------------+-------------------+----------------------------------+
| host0002 | 1-2:2  | cdb       | cdb$root | 29213 | open            | normal       | standby  | 1-1:1       | 192.168.1.3:1688  | /data/yashan/yasdb_data/db-1-2   |
+----------+--------+-----------+----------+-------+-----------------+------------+----------+-------------+-------------------+----------------------------------+
| host0003 | 1-3:3  | cdb       | cdb$root | 49582 | open            | normal       | standby  | 1-2:2       | 192.168.1.4:1688  | /data/yashan/yasdb_data/db-1-3   |
+----------+--------+-----------+----------+-------+-----------------+------------+----------+-------------+-------------------+----------------------------------+
```

    == mysql模式

```shell
+--------------------------------------------------------------------------------------------------------------------------------------------------------------+
| hostid   | node_type | nodeid | pid   | instance_status | database_status | database_role | source_node | listen_address  | data_path                        |
+--------------------------------------------------------------------------------------------------------------------------------------------------------------+
| host0001 | db        | 1-1:1  | 73152 | open            | normal          | primary       | -           | 192.168.1.2:1688 | /data/yashan/yasdb_data/db-1-1  |
+----------+-----------+--------+-------+-----------------+-----------------+---------------+-------------+------------------+---------------------------------+
| host0002 | db        | 1-2:2  | 73211 | open            | normal          | standby       | 1-1:1       | 192.168.1.3:1688 | /data/yashan/yasdb_data/db-1-2  |
+----------+-----------+--------+-------+-----------------+-----------------+---------------+-------------+------------------+---------------------------------+
| host0003 | db        | 1-3:3  | 73403 | open            | normal          | standby       | 1-2:2       | 192.168.1.4:1688 | /data/yashan/yasdb_data/db-1-3  |
+----------+-----------+--------+-------+-----------------+-----------------+---------------+-------------+------------------+---------------------------------+
```
    :::



2. 使用[yasql](../../全部手册/工具手册/yasql/yasql使用指导)工具连接数据库，查看实例状态。

    ```shell
    $ yasql sys/********@192.168.1.2:1688
    SQL> SELECT STATUS FROM V$INSTANCE;

    STATUS        
    ------------- 
    OPEN        

    SQL> SELECT database_name FROM v$database;

    DATABASE_NAME                                                    
    ---------------------------------------------------------------- 
    yashandb   
    ```



### 步骤6：启动守护进程


执行如下命令，启动守护进程。

```shell
$ yasboot monit start --cluster yashandb
```

> **Note**:
>
> 后续使用中服务器重启开机后需手动启动守护进程，守护进程会自动拉起数据库相关进程，从而启动数据库。为简化运维，可将守护进程配置为开机自启动间接实现数据库开机自启动，具体操作请查阅[配置开机自启动](../../全部手册/安装和升级/安装部署/安装后初始环境/配置开机自启动)。



## 双复制组主备部署

双复制组主备部署将数据库集群划分为分为主复制组和备复制组，每个组对应的服务器安装在不同的地域/机房。其中，主复制组应提供3个及以上节点，以部署为一主多备形态；备复制组应提供2个及以上节点，以部署为一个备库+级联备的形态。


请以安装用户（以yashan为例）登录到已下载软件包的服务器（本文以192.168.1.2为例），并进入软件包所在的安装目录（以/home/yashan/install为例）。

```shell
$ cd /home/yashan/install
```



### 步骤1：生成配置文件


本步骤用于生成YashanDB产品的两个重要的配置文件：hosts.toml和{集群名称}.toml，其中，集群名称来自于--cluster选项的参数值，本文中为yashandb，该配置文件在下文均称为yashandb.toml。

hosts.toml和yashandb.toml中定义了YashanDB的部署信息和建库信息，也被用于产品之后的扩缩容和升级等操作，因此一旦确认（即部署完成并投入运行）后请不要进行手工调整。

在未进行下一步骤前，本步骤的命令可重复运行，每次运行将直接覆盖之前生成的hosts.toml和yashandb.toml文件。

1. 执行yasboot package se gen命令生成配置文件，该命令的参数全集请查阅[yasboot文档](../../全部手册/工具手册/yasboot/yasboot命令介绍/yasboot package)。

    以下所展示的命令示例包含了命令的必输入选项，以及我们建议您输入的选项，请根据实际情况自定义选项参数。

    请注意--ip选项后面的值应按照主复制组主、主复制组备、备复制组备、备复制组级联备的顺序进行输入。多个IP地址以`,`逐一输入，连续IP地址则可以使用范围格式输入，例如192.168.1.[2-4]。

    此外，对于双复制组主备部署，--group选项值应输入为2，--node选项的值将作为主复制组中的主+备数量，--standby-node选项的值将作为备复制组中的备+级联备的数量。

    以下命令以主复制组一主两备，备复制组一备三级联备为例，请根据实际情况调整参数。

    :::tabs
    == 默认示例场景

```shell
$ ./bin/yasboot package se gen --cluster yashandb \
-u yashan -p yashan_password  \
--ip 192.168.1.2,192.168.1.3,192.168.1.4,192.168.10.5,192.168.10.6,192.168.10.7 --port 22 \
--install-path /data/yashan/yasdb_home  \
--data-path /data/yashan/yasdb_data \
--log-path /data/yashan/log \
--begin-port 1688 \
--group 2 --node 3 --standby-node 3
```

    == 租户模式

```shell
$ ./bin/yasboot package se gen --cluster yashandb \
-u yashan -p yashan_password  \
--ip 192.168.1.2,192.168.1.3,192.168.1.4,192.168.10.5,192.168.10.6,192.168.10.7 --port 22 \
--install-path /data/yashan/yasdb_home  \
--data-path /data/yashan/yasdb_data \
--log-path /data/yashan/log \
--begin-port 1688 \
--group 2 --node 3 --standby-node 3 \
--enable-pluggable-database
```

    == mysql模式

```shell
$ ./bin/yasboot package se gen --cluster yashandb \
-u yashan -p yashan_password  \
--ip 192.168.1.2,192.168.1.3,192.168.1.4,192.168.10.5,192.168.10.6,192.168.10.7 --port 22 \
--install-path /data/yashan/yasdb_home  \
--data-path /data/yashan/yasdb_data \
--log-path /data/yashan/log \
--begin-port 1688 \
--group 2 --node 3 --standby-node 3 \
--mode mysql
```

    == 多网段

下述命令只适用于所有主备复制链路地址都位于同一物理网段的情况；否则应在配置文件中进行手工调整。

```shell
$ ./bin/yasboot package se gen --cluster yashandb \
-u yashan -p yashan_password  \
--ip 192.168.1.2,192.168.1.3,192.168.1.4,192.168.10.5,192.168.10.6,192.168.10.7 --port 22 \
--install-path /data/yashan/yasdb_home  \
--data-path /data/yashan/yasdb_data \
--log-path /data/yashan/log \
--begin-port 1688 \
--group 2 --node 3 --standby-node 3 \
--replica-cidr 172.16.1.0/24
```
    :::



    执行完毕后，当前目录下将生成hosts.toml和yashandb.toml两个配置文件。

2. 请确认您在上一步所输入的生成配置文件命令是否对应了您所需要的[安装场景](#InstallationScenario)。

    如需修改场景，可按对应场景的操作指导调整上一步所输入的生成配置文件命令，重新运行，再次生成配置文件。

3. 根据实际需要，手动修改配置文件中的相关参数。

    >**Caution**:
    >
    > 不建议删除文件中任何行，否则可能导致后续安装过程报错或所搭建的环境后续无法进行扩展配置。

    以下列示部分可能需要调整的参数项，完整的配置文件内容说明请查阅[配置文件模板](../../全部手册/工具手册/yasboot/配置文件/00配置文件)介绍，请根据实际情况选择参数项调整值。

    :::tabs
    == hosts.toml



| 参数项| 调整说明|
| ------------------------------------------ | ------------------------------------------------------------ |
| 所有的LISTEN_ADDR                           | yasom服务和yasagent服务所在地址和监听端口，请检查确保与实际开放的端口号一致，可修改为实际值             |



    == yashandb.toml



| 参数项| 调整说明|
| ------------------------------------------ | ------------------------------------------------------------ |
| table_type                                 | 主要的业务表类型，默认HEAP，mysql模式不可调整                |
| recommend_param/scale_factor/scene         | 指定了--recommend-param选项时，可进一步调整推荐参数的选项，具体请参考[DBMS_PARAM](../../全部手册/开发手册/PL参考手册/内置高级包/DBMS_PARAM)高级包文档描述。 |
| data_path                                 | YAS_DATA环境变量对应值，默认各节点的数据路径完全相同，不建议调整<br>如主备节点的数据路径不一致，则必须同时对所有节点上的DB_FILE_NAME_CONVERT、REDO_FILE_NAME_CONVERT、DB_BUCKET_NAME_CONVERT参数值进行调整             |
| mysql_addr                                 | yashan模式无此参数<br>MySQL协议的监听地址和端口，请检查确保与实际开放的端口号一致，可修改为实际值             |
| role         | 该参数定义了节点实例的主备和级联备角色，如发现与实际不一致，建议重新指定--ip、--node、--cascade-node以及--cascade-parent选项运行生成配置文件命令            |
| [group.config]下各参数                     | 该类参数均为[建库关键参数](../../全部手册/工具手册/yasboot/建库参数)，未在文件中列出的参数均采用默认值，生产环境请务必按实际情况进行调整。 |
| [group.node.config]下各参数                | 该类参数均为本节点所部署实例的[数据库配置参数](../../全部手册/参考手册/配置参数)，除以下参数外未在文件中列出的参数均采用默认值，生产环境请务必按实际情况进行调整。 |
| [group.config.SEED]下各参数                | 非租户模式无该类参数<br>该类参数用于定义种子容器的属性，默认继承根容器属性，如需自定义PDB创建模板，请在此处调整参数选项值。 |
| [group.node.mysql_config]下各参数          | 该类参数均为mysql模式下的[系统变量](../../全部手册/mysql模式参考手册/系统变量/系统变量参考)（yashan模式无需理会），未在文件中列出的参数均采用默认值，生产环境请务必按实际情况进行调整。 |
| REPLICATION_ADDR    | 该参数定义了主备复制链路地址和端口<br>端口号默认为LISTEN_ADDR的端口号+1，请确保与实际开放的端口号一致，可修改为实际值<br>多网段场景中，该参数的地址部分须留空，例如":1689"           |
| [[host]]下各参数    | 未指定--replica-cidr时，配置文件中不会生成该类参数<br>指定了每个节点的私网IP地址，可手工增加该类参数，或对已生成的参数进行调整        |



> **Note**:
> 
> HA_ELECTION_ENABLED参数为自动选主功能的总开关，不可直接在本处修改，主复制组各节点的该参数自动且强制开启。如需关闭只能在安装完成后参照[一主多备自动选主](../../全部手册/高可用/自动选主配置/一主多备自动选主)文档手动操作。
> 
> 备复制组不参与自动选主，所有节点必须关闭自动选主功能，请保持HA_ELECTION_ENABLED参数为默认值FALSE。

    :::
 
### 步骤2：执行安装


执行如下命令，执行软件包安装。

```shell
$ ./bin/yasboot package install -t hosts.toml
```

|  参数| 说明|
|--------------------|--------------------------------------|
| -t, --toml | 部署数据库集群的hosts.toml路径 |



### 步骤3：部署数据库


执行如下命令，部署数据库。

```shell
$ ./bin/yasboot cluster deploy -t yashandb.toml -p sys_password
```

|  参数| 说明|
|--------------------|--------------------------------------|
| -t, --toml | 部署数据库集群的{集群名称}.toml文件路径 |
| -p, --sys-password |设置数据库超级管理员sys用户的密码，配置要求如下：<br/>* 密码长度为8 - 64位<br/>* 密码中不能包含对应的数据库用户名称<br/>* 密码必须同时包含数字、字母和特殊字符<br/>* OS命令相关的特殊字符（例如`@`、`/`、`.`、`!`、`$`、`'`等）需进行转义  |

> **Note**:
>
> 还可以通过指定`-d, --child`参数展示任务以及子任务信息，以便了解部署进度。

返回如下信息表示已成功部署。

```shell
task completed, status: SUCCESS
```



### 步骤4：配置环境变量




部署成功后，--install-path（例如/data/yashan/yasdb_home）指定的目录下会生成子目录`{version_number}/conf`，该目录下会自动生成YashanDB相关的环境变量文件`{集群名称}.bashrc`，需将其应用于操作系统。

1. 执行如下命令，生效环境变量。

    ```shell
    # 进入环境变量文件所在目录，例如/data/yashan/yasdb_home/{version_number}/conf
    $ cd /data/yashan/yasdb_home/{version_number}/conf

    # 生效环境变量
    $ cat yashandb.bashrc >> ~/.bashrc
    $ source ~/.bashrc
    
    # 校验环境变量是否生效（回显信息中的路径请以实际为准）
    $ echo $YASDB_DATA
    /data/yashan/yasdb_data/db-1-1
    ```

    具体的环境变量介绍请查阅[安装后初始环境 > 环境变量](../../全部手册/安装和升级/安装部署/安装后初始环境/环境变量)。



2. 以安装用户登录当前环境中的其他服务器，重复上述操作生效环境变量。


### 步骤5：检查安装结果


若连接报错或执行SQL语句报错，请根据错误提示信息检查安装步骤，或咨询我们的技术支持。

1. 执行如下命令，查看数据库状态。

    ```shell
    $ yasboot cluster status -c yashandb -d
    ```
        
    回显信息示例如下：

    如下输出在多网段场景中完全一致。

    :::tabs
    == 默认示例场景

```shell
+----------------------------------------------------------------------------------------------------------------------------------------------------------------+
| hostid   | node_type | nodeid | pid   | instance_status | database_status | database_role | source_node | listen_address   | data_path                         |
+----------------------------------------------------------------------------------------------------------------------------------------------------------------+
| host0001 | db        | 1-1:1  | 73132 | open            | normal          | primary       | -           | 192.168.1.2:1688  | /data/yashan/yasdb_data/db-1-1    |
+----------+-----------+--------+-------+-----------------+-----------------+---------------+-------------+-------------------+-----------------------------------+
| host0002 | db        | 1-2:2  | 73199 | open            | normal          | standby       | 1-1:1       | 192.168.1.3:1688  | /data/yashan/yasdb_data/db-1-2    |
+----------+-----------+--------+-------+-----------------+-----------------+---------------+-------------+-------------------+-----------------------------------+
| host0003 | db        | 1-3:3  | 73201 | open            | normal          | standby       | 1-1:1       | 192.168.1.4:1688  | /data/yashan/yasdb_data/db-1-3    |
+----------+-----------+--------+-------+-----------------+-----------------+---------------+-------------+-------------------+-----------------------------------+
| host0004 | db        | 2-1:4  | 73362 | open            | normal          | standby       | 1-1:1       | 192.168.10.5:1688 | /data/yashan/yasdb_data/db-2-1    |
+----------+-----------+--------+-------+-----------------+-----------------+---------------+-------------+-------------------+-----------------------------------+
| host0005 | db        | 2-2:5  | 73435 | open            | normal          | standby       | 2-1:4       | 192.168.10.6:1688 | /data/yashan/yasdb_data/db-2-2    |
+----------+-----------+--------+-------+-----------------+-----------------+---------------+-------------+-------------------+-----------------------------------+
| host0006 | db        | 2-3:6  | 73499 | open            | normal          | standby       | 2-1:4       | 192.168.10.7:1688 | /data/yashan/yasdb_data/db-2-3    |
+----------+-----------+--------+-------+-----------------+-----------------+---------------+-------------+-------------------+-----------------------------------+
```
    == 租户模式

```shell
+-----------------------------------------------------------------------------------------------------------------------------------------------------------------+
| hostid   | nodeid | node_type | pdb_name | pid   | instance_status | pdb_status | pdb_role | source_node | listen_address    | data_path                        |
+-----------------------------------------------------------------------------------------------------------------------------------------------------------------+
| host0001 | 1-1:1  | cdb       | cdb$root | 73132 | open            | normal       | primary  | -         | 192.168.1.2:1688  | /data/yashan/yasdb_data/db-1-1   |
+----------+--------+-----------+----------+-------+-----------------+------------+----------+-------------+-------------------+----------------------------------+
| host0002 | 1-2:2  | cdb       | cdb$root | 73199 | open            | normal       | standby  | 1-1:1     | 192.168.1.3:1688  | /data/yashan/yasdb_data/db-1-2   |
+----------+--------+-----------+----------+-------+-----------------+------------+----------+-------------+-------------------+----------------------------------+
| host0003 | 1-3:3  | cdb       | cdb$root | 73201 | open            | normal       | standby  | 1-1:1     | 192.168.1.4:1688  | /data/yashan/yasdb_data/db-1-3   |
+----------+--------+-----------+----------+-------+-----------------+------------+----------+-------------+-------------------+----------------------------------+
| host0003 | 2-1:4  | cdb       | cdb$root | 73362 | open            | normal       | standby  | 1-1:1     | 192.168.10.5:1688 | /data/yashan/yasdb_data/db-2-1   |
+----------+--------+-----------+----------+-------+-----------------+------------+----------+-------------+-------------------+----------------------------------+
| host0003 | 2-2:5  | cdb       | cdb$root | 73435 | open            | normal       | standby  | 2-1:4     | 192.168.10.6:1688 | /data/yashan/yasdb_data/db-2-2   |
+----------+--------+-----------+----------+-------+-----------------+------------+----------+-------------+-------------------+----------------------------------+
| host0003 | 2-3:6  | cdb       | cdb$root | 73499 | open            | normal       | standby  | 2-1:4     | 192.168.10.7:1688 | /data/yashan/yasdb_data/db-2-3   |
+----------+--------+-----------+----------+-------+-----------------+------------+----------+-------------+-------------------+----------------------------------+
```

    == mysql模式

```shell
+----------------------------------------------------------------------------------------------------------------------------------------------------------------+
| hostid   | node_type | nodeid | pid   | instance_status | database_status | database_role | source_node | listen_address   | data_path                         |
+----------------------------------------------------------------------------------------------------------------------------------------------------------------+
| host0001 | db        | 1-1:1  | 73132 | open            | normal          | primary       | -           | 192.168.1.2:1688  | /data/yashan/yasdb_data/db-1-1    |
+----------+-----------+--------+-------+-----------------+-----------------+---------------+-------------+-------------------+-----------------------------------+
| host0002 | db        | 1-2:2  | 73199 | open            | normal          | standby       | 1-1:1       | 192.168.1.3:1688  | /data/yashan/yasdb_data/db-1-2    |
+----------+-----------+--------+-------+-----------------+-----------------+---------------+-------------+-------------------+-----------------------------------+
| host0003 | db        | 1-3:3  | 73201 | open            | normal          | standby       | 1-1:1       | 192.168.1.4:1688  | /data/yashan/yasdb_data/db-1-3    |
+----------+-----------+--------+-------+-----------------+-----------------+---------------+-------------+-------------------+-----------------------------------+
| host0004 | db        | 2-1:4  | 73362 | open            | normal          | standby       | 1-1:1       | 192.168.10.5:1688 | /data/yashan/yasdb_data/db-2-1    |
+----------+-----------+--------+-------+-----------------+-----------------+---------------+-------------+-------------------+-----------------------------------+
| host0005 | db        | 2-2:5  | 73435 | open            | normal          | standby       | 2-1:4       | 192.168.10.6:1688 | /data/yashan/yasdb_data/db-2-2    |
+----------+-----------+--------+-------+-----------------+-----------------+---------------+-------------+-------------------+-----------------------------------+
| host0006 | db        | 2-3:6  | 73499 | open            | normal          | standby       | 2-1:4       | 192.168.10.7:1688 | /data/yashan/yasdb_data/db-2-3    |
+----------+-----------+--------+-------+-----------------+-----------------+---------------+-------------+-------------------+-----------------------------------+
```
    :::



2. 使用[yasql](../../全部手册/工具手册/yasql/yasql使用指导)工具连接数据库，查看实例状态。

    ```shell
    $ yasql sys/********@192.168.1.2:1688
    SQL> SELECT STATUS FROM V$INSTANCE;

    STATUS        
    ------------- 
    OPEN        

    SQL> SELECT database_name FROM v$database;

    DATABASE_NAME                                                    
    ---------------------------------------------------------------- 
    yashandb   
    ```



### 步骤6：启动守护进程


执行如下命令，启动守护进程。

```shell
$ yasboot monit start --cluster yashandb
```

> **Note**:
>
> 后续使用中服务器重启开机后需手动启动守护进程，守护进程会自动拉起数据库相关进程，从而启动数据库。为简化运维，可将守护进程配置为开机自启动间接实现数据库开机自启动，具体操作请查阅[配置开机自启动](../../全部手册/安装和升级/安装部署/安装后初始环境/配置开机自启动)。


