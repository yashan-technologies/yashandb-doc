yfscmd是YFS管理工具，用户可使用本工具实现对YFS的基本管理，如文件、目录的增删查改等。

在执行yfscmd子命令（包括[磁盘管理命令](磁盘管理命令)和[文件管理命令](文件管理命令)）时，必须保证集群管理服务（YCS）已正常启动和运行中，否则命令无法成功执行。

yfscmd所运行环境也依赖于YCS的环境变量`$YASCS_HOME`，必须保证该环境变量已被正确设置。

yfscmd工具提供了部分实验室特性，例如别名、bash、部分文件管理命令等，其中部分高危指令会改变共享存储上的数据，错误的操作会损坏、丢失数据库的数据文件，**不推荐在生产环境中使用**。
