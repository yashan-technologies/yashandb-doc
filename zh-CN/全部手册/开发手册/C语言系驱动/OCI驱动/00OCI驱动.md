YashanDB OCI驱动允许客户创建使用函数调用访问数据库并控制SQL语句执行和数据访问的所有阶段的应用程序，支持C和C++的数据类型、调用约定、语法和语义。仅当YashanDB部署为yashan模式时支持使用该驱动进行连接。

OCI驱动支持的操作系统+CPU架构组合如下：

- Windows 7~11 + x64

- Linux + ARM64

- Linux + LoongArch64

- Linux + x86_64
