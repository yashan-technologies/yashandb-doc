SSL连接在普通的TCP连接之上增加了SSL协议构建[可信信道](../../../产品安全/加密支持/可信信道)，用于实现数据库客户端与服务端之间的通信加密。

经过可信信道的所有通讯内容都是加密传输的，例如客户端向服务端发起的SQL请求，服务端向客户端返回的查询结果数据等。

YashanDB支持对多种网络链路启用SSL可信信道，包括[数据库服务端与客户端之间](数据库服务端SSL可信信道)、[HA节点之间](HA节点间SSL可信信道)、[分布式节点之间](分布式节点间SSL可信信道)。
