通用描述
----

CREATE FUNCTION用于创建一个自定义函数（UDF）。

自定义函数为PL对象，对其的创建需采用过程体语言实现，且需满足YashanDB所实现PL（包括变量、参数、语句等）的一切规范和约束，具体描述请查看[PL参考手册](../../全部手册/开发手册/PL参考手册/00PL参考手册)。

关于CREATE FUNCTION的语法描述详见PL参考手册的[自定义函数](../../全部手册/开发手册/PL参考手册/PL对象/自定义函数)。
