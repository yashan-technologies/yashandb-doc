## 用户表空间集管理准则

表空间集（TABLESPACE SET）是存算一体分布式数据库中一个逻辑存储单位，在存算一体分布式集群部署中用于存储分布表、建立在分布表上的索引、建立在分布表上的ac等。

用户创建分布表时可为其指定TABLESPACE SET，如果不指定则使用默认的TABLESPACE SET。

## YashanDB默认表空间集

YashanDB内置了如下表空间集，其属性可以默认或者在建库时指定：

- USERS表空间集
- USERS_AIM表空间集
