## Connection Object

This object is the database connection object, corresponding to the actual connection between the Python application and the database, and is used to execute operations at the entire database level.

It is created by the connect function provided by the python-yasdb module.

### Supported Methods

|Method |
| ---------- |
| close()    |
| commit()   |
| rollback() |
| cursor()   |
