```ebnf
vector_dims = VECTOR_DIMS "(" expr ")".
```

The VECTOR_DIMS function is used to obtain the dimension count of a vector, and the return value of this function is of NUMBER type.

The VECTOR_DIMS function has the same meaning as the [VECTOR_DIMENSION_COUNT](./VECTOR_DIMENSION_COUNT) function.
