In YAC Deployment, users can scale cluster instances as needed in a running environment, which means adding instances to a specific cluster (including YCS instances and database instances).

>**Note**:
>
> - Currently, CDBs cannot scale instances within the cluster and backup clusters.
>
> - Currently, it is not possible to add or remove instances within the cluster, and scaling of backup clusters is not available.
