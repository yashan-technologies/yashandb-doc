View Help and Version Information
----

Use the exp -h or exp --help command to display help and version information and exit.

```shell
$ exp -h

# Or:
$ exp --help
```

Use the exp -v command to display version information and exit.

```shell
$ exp -v
```
